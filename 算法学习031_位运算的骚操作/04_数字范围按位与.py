""" 计算区间[left, right]内所有整数的按位与结果 """


# 力扣测试连接:https://leetcode.cn/problems/bitwise-and-of-numbers-range/

class Solution:
    def rangeBitwiseAnd(self, left: int, right: int) -> int:
        while left < right:
            right -= right & -right
        return right

# 🔍 功能说明:计算区间 [left, right] 内所有整数的按位与结果
#    例如：left = 5, right = 7，则计算 5 & 6 & 7 = 4
#    
# 🔄 核心思想:消除所有会变化的低位,保留公共前缀
#    1. 在区间 [left, right] 的连续整数中,低位比特必然变化(0→1或1→0)
#    2. 只要某位出现过0,按位与结果中该位就是0
#    3. 只有高位公共前缀保持不变,这些位在按位与中保持为1
#    
# 🎯 关键操作:right & -right
#    - -right 是 right 的二进制补码(取反加1)
#    - right & -right 获取 right 二进制表示中最低位的1
#    - 例如：right = 10110₂, -right = 01010₂, right & -right = 00010₂
#    
# 🧹 算法过程:
#    循环消去 right 最低位的1,直到 right ≤ left
#    此时的 right 就是 left 和 right 的公共前缀
#    这个公共前缀就是区间所有数字按位与的结果
#    
# ⏱ 时间复杂度:O(k),其中k是right中1的位数
# 💾 空间复杂度:O(1)
